# Python virtual environment
.venv/
__pycache__/
*.pyc

# Components fetched by --fetch / bundled into a build. Running the app from
# source puts these in the repo folder, and they are gigabytes each.
models/
nvidia/

# Build output
build/
dist/

# User runtime config (defaults live in whispertype.py)
config.json
