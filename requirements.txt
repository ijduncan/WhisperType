faster-whisper==1.1.1
sounddevice==0.5.1
numpy==2.1.3
keyboard==0.13.5
pyperclip==1.9.0
pystray==0.19.5
pillow==11.1.0
requests==2.34.2

# GPU acceleration lives in requirements-gpu.txt. It is ~1.3 GB and is only
# needed to run from source on a GPU, or to build the bundled installer --
# the shipped installer downloads those libraries itself.
